# ============================================
# VerduleriaCatrinacio - Configuración General
# ============================================

spring.application.name=verduleria-catrinacio

# Perfil activo (dev por defecto)
spring.profiles.active=dev

# Servidor
server.port=8080
server.servlet.context-path=/api
